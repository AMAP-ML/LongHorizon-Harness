"""v7 — runtime recursive decomposition (PLAN.md §A8, §B5).

Empty on purpose: every v0-v6 ``__init__.py`` in this repo is a bare
namespace marker, and v7 follows the same convention rather than
re-exporting symbols a caller can import directly from ``split``.
"""

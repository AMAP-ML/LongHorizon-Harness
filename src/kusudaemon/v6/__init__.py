"""v6 — the work object (PLAN.md §A3, §B1).

Empty on purpose: every v0-v5 ``__init__.py`` in this repo is a bare
namespace marker, and v6 follows the same convention rather than
re-exporting symbols a caller can import directly from ``work_object``.
"""
